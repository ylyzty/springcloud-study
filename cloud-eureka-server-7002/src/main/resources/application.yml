server:
  port: 7002

eureka:
  instance:
    hostname: eureka7002.com  #eureka服务端实例
  client:
    register-with-eureka: false  #表示不注册自己
    fetch-registry: false   #表示职责是维护实例，不需要检索服务
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/